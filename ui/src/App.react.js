import React, { Component } from 'react';
import AppBar from 'material-ui/AppBar';

export default class App extends Component {
  render() {
    return (
      <AppBar title="Experimento UI" />
    );
  }
}
